{
  "name": "monorepoproject",
  "private": true,
  "workspaces": [
    "front",
    "server"
  ],
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "f_dev": "npm --prefix front run dev",
    "f_build": "npm --prefix front run build",
    "s_dev": "npm --prefix server run start:dev",
    "s_build": "npm --prefix server run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run f_build && npm run s_build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
